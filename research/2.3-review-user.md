# LLMs Answers Review (TO-DO)

Below is a summary of each AI's response, excluding statements that were already present in my initial prompt.

## GPT answer:

- Treats `/context` as the agent “world model,” framework‑agnostic.
- Proposes a minimal front‑matter schema and a PR checklist to nudge context updates.
- Introduces a diff‑aware "Context Steward" role plus optional lint/workflow hooks.
- Clarifies a clean split between tech, development, and operations docs.

## Claude answer:

- Adds an `agents/capabilities.md` catalog and ties to AI Toolkit guidance.
- Standardizes front‑matter (incl. `updated-by`) and stresses freshness discipline.
- Supplies a concrete freshness‑check Action and a periodic review agent spec.
- Keeps scope explicit for single‑repo solos; separates ops from core tech docs.

## Gemini answer:

- Recasts the system as “Systemic Memory” with Continuous Flow; uses `execution/` with `active.md` and a changelog.
- Elevates `AGENTS.md` into a routing protocol with pre/post steps and tag‑based lookup.
- Suggests warning‑only drift checks and a local diff‑driven `context:audit` flow.
- Expands product docs (flows, UX patterns, copy dictionary) for agent consumption.

# Final Thoughts

- Sprints now are called cycles and the folder to hold them is `operations`.
- Automations that we'll consider are
- We must mention that the solopreneurs we target are also the developers.+
- Front-matter fields should be `context_type`, `status`, and `last_updated`. No owner field.
