Business context lives in `/context/`, managed by the repo-business-context skill — consult it before business-flavored work (copy, pricing, positioning, features, strategy).

This repo is also the source of that skill: `skills/repo-business-context/` is the distributed source of truth; `.agents/skills/` is the local install (gitignored) — keep them in sync when editing the skill.
